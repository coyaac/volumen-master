Fija el volumen objetivo de un stream. La cifra va siempre en escala del propio stream, nunca en porcentaje.

```jsx
<VolumeSlider icon="volume_up" label="Música" value={12} max={15} onChange={setMusica} />
```

Objetivo táctil de 48dp aunque la barra mida 16dp.
