Interruptor de dos estados. El grande, con `onColor="var(--vl-locked)"`, es el candado maestro de la pantalla principal.

```jsx
<Switch size="large" checked={bloqueado} onChange={setBloqueado} onColor="var(--vl-locked)" />
```
