Encabeza la pantalla principal. Es el único elemento que cambia de color con el estado.

```jsx
<LockHero locked={bloqueado} since="08:14" onToggle={setBloqueado} />
```

Verde `--vl-locked-container` bloqueado, neutro desbloqueado. Nunca uses el índigo primario aquí.
