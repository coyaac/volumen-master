Confirmaciones que no se pueden deshacer, como borrar el historial.

```jsx
<Dialog open={abierto} icon="delete" title="¿Borrar el historial?"
  actions={<><Button variant="text">Cancelar</Button><Button variant="danger">Borrar</Button></>}>
  Se eliminarán los 218 registros guardados.
</Dialog>
```
