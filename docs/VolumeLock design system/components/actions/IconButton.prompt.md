Botón sólo icono, 48dp de objetivo táctil; para acciones de barra superior y de fila.

```jsx
<IconButton icon="filter_list" label="Filtrar por fecha" onClick={abrirFiltro} />
```

`selected` lo rellena (eje FILL a 1) y le da fondo de contenedor primario.
